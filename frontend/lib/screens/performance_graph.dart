import 'package:flutter/material.dart';

class PerformanceGraph extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Graph Placeholder")),
    );
  }
}
